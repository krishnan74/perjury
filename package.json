{
  "name": "perjury",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "agents/*", "app"],
  "scripts": {
    "test": "vitest run",
    "test:contracts": "forge test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.2"
  }
}
